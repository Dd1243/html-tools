# 编码与 JavaScript 修复实施计划

**目标：** 清除生产页面与模板中的 Unicode 替换字符，并修复 ESLint 报告的 JavaScript 错误。

**架构：** 对近期才损坏的模板与离线页从最后干净提交恢复；对已改版工具页使用初始干净版本逐区块恢复文字与脚本；对不涉及编码的 JavaScript 错误做最小语法修复。新增源码完整性测试防止替换字符再次进入仓库。

**技术栈：** 静态 HTML、内联 JavaScript、Node.js 测试、ESLint、HTMLHint。

---

### 任务 1：源码完整性回归测试
- 创建 `tests/source-integrity.test.js`，扫描生产 HTML 和模板中的 `U+FFFD`。
- 将测试加入 `package.json` 默认测试命令。
- 运行测试并确认因现有乱码失败。

### 任务 2：恢复近期编码损坏
- 从 `2d8a0173` 恢复 `offline.html` 和三个模板的正确字符。
- 保留当前模板结构及有效后续变更，仅替换损坏内容。

### 任务 3：修复长期乱码工具页
- 以 `f3685290` 为可信原文，修复六个工具页中的损坏字符和相关脚本区块。
- 保留当前 canonical、元描述、页脚和后续功能变更。

### 任务 4：修复 JavaScript 错误
- 修复 ESLint 报告的解析、重复声明、精度和逻辑表达式错误。
- 不改动与错误无关的页面行为。

### 任务 5：验证
- 运行源码完整性测试、`npm run lint:js`、受影响文件 HTMLHint 和 `npm test`。
- 检查 Git diff，确保没有整页意外回退。