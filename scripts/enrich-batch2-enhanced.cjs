/**
 * Batch 2: add enhanced (800-1200 CN) function-verified guides to 8 thin pages.
 */
const fs = require('fs');
const path = require('path');
const root = path.join(__dirname, '..');

const style =
  'margin:2rem auto;max-width:760px;padding:1.35rem 1.4rem 1.6rem;border:1px solid var(--border-color,var(--border,#333));border-radius:12px;line-height:1.8;background:var(--bg-card,rgba(255,255,255,0.02));color:var(--text-primary,inherit)';

function section(inner) {
  return `<section class="tool-guide" aria-label="使用指南" style="${style}">\n${inner}\n</section>`;
}
function cn(s) {
  return (String(s).replace(/<[^>]+>/g, ' ').match(/[\u4e00-\u9fff]/g) || []).length;
}

const PAGES = {
  'tools/office/meeting-scheduler.html': {
    anchor: '<!-- SEO 内容 -->',
    guide: section(`
  <h2 style="font-size:1.25rem;margin:0 0 .85rem">跨时区会议协调完整使用指南</h2>
  <p style="margin:0 0 1rem">本工具用“固定 UTC 小时轴 + 每人 UTC 偏移”计算共同可开会时段。默认预置本地团队（UTC+8）、欧洲团队（UTC+1）、美东团队（UTC-5）。你可添加参会者姓名与时区偏移，网格会显示 00:00–23:00（UTC）下每人本地时刻；本地小时落在 9:00–18:00 记为可开会，否则为非工作时间。所有人同时可开会的 UTC 小时会标成 MATCH，并在结果区提示是否发现理想时段。配置可写入 <code>localStorage.meeting_scheduler_state</code>，也可生成 Base64 哈希分享链接。</p>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">界面与真实行为</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>输入姓名 <code>#pName</code>，选择 <code>#pTimezone</code> 的 UTC 偏移（含 5.5、9.5 等半点）。</li>
    <li>点「添加参会者」把 <code>{name, tz}</code> 推入数组并 <code>render()</code>。</li>
    <li>芯片列表显示姓名与 UTC±N，点 × 调用 <code>removeParticipant(i)</code>。</li>
    <li>网格首行是 UTC 小时头；每人一行显示换算后的本地整点。</li>
    <li>工作时间判定：<code>localHr &gt;= 9 &amp;&amp; localHr &lt; 18</code>（不区分周末/午休）。</li>
    <li>「复制分享链接」：把 participants JSON 做 <code>btoa(encodeURIComponent(...))</code> 放进 URL hash。</li>
    <li>「清空」需确认，会清空数组并 removeItem 本地状态。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">推荐步骤</h2>
  <ol style="margin:0 0 1rem;padding-left:1.2rem">
    <li>先保留或调整默认三方，再添加真实参会人与偏移。</li>
    <li>看最底“共同可用时段 (UTC)”哪些格子是 MATCH。</li>
    <li>把匹配的 UTC 小时换算成你本地时间后发会议邀请。</li>
    <li>需要协作时点复制分享链接，让对方打开同一配置。</li>
    <li>无重叠时，考虑异步沟通或让一方让步休息时间（页面会给出警告文案）。</li>
  </ol>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">适用场景</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>中美欧研发周会、客户成功跨区排期</li>
    <li>远程面试多方协调</li>
    <li>快速验证“有没有一个大家都不加班的小时”</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">边界与限制</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>使用固定 UTC 偏移，不自动处理夏令时切换细节。</li>
    <li>工作时间硬编码 9–18，不能按人设置班次。</li>
    <li>半点时区用 <code>parseFloat</code>，本地显示用 <code>Math.floor(localHr)</code>，半点展示为向下取整的整点标签。</li>
    <li>不是日历写入工具：不会创建 Google/Outlook 事件。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">常见问题</h2>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">为什么有人显示可开会，共同行却没有 MATCH？</h3>
  <p style="margin:0 0 .8rem">共同行要求该 UTC 小时对<strong>所有</strong>参会者都是工作时间；任意一人忙碌就会取消重叠。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">分享链接里有什么？</h3>
  <p style="margin:0 0 .8rem">仅有参会者姓名与时区偏移的编码，不含账号密码；仍避免在链接里写敏感真名若你有合规要求。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">刷新后配置还在吗？</h3>
  <p style="margin:0 0 .8rem">优先读 URL hash；否则读 <code>meeting_scheduler_state</code>；都没有则回默认三方示例。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">能按 30 分钟粒度吗？</h3>
  <p style="margin:0 0 .8rem">当前网格是 1 小时一格，不支持 15/30 分钟切片。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">空姓名能添加吗？</h3>
  <p style="margin:0">不能。姓名 <code>trim()</code> 后为空会忽略添加。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">这能替代世界时钟吗？</h3>
  <p style="margin:0">不能。世界时钟看多城实时时刻；本页专做“共同工作小时”匹配。</p>
`),
  },

  'tools/dev/json-to-go.html': {
    anchor: '<!-- SEO 内容 -->',
    guide: section(`
  <h2 style="font-size:1.25rem;margin:0 0 .85rem">JSON 转 Go 结构体完整指南</h2>
  <p style="margin:0 0 1rem">本工具把 JSON 样例转换成 Go 的 <code>struct</code> 定义，便于对接 API。左侧粘贴 JSON，右侧得到 Go 代码。可设置根结构体名称（默认 <code>AutoGenerated</code>），勾选「添加 omitempty 标签」与「内联类型定义」。提供「转换」「复制代码」「清空」以及示例按钮。字段名会经 <code>toGoName</code> 转成 PascalCase 导出字段，并生成 <code>json:"原键名"</code> 标签。</p>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">类型推断规则（与脚本一致）</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li><code>null</code> → <code>interface{}</code></li>
    <li>字符串 → <code>string</code>；布尔 → <code>bool</code></li>
    <li>数字：整数 → <code>int64</code>，非整数 → <code>float64</code></li>
    <li>数组：空数组 → <code>[]interface{}</code>；否则取首元素推断 <code>[]T</code></li>
    <li>对象：生成命名结构体；开启内联时写成匿名 <code>struct { ... }</code></li>
    <li>omitempty：勾选后标签变为 <code>json:"key,omitempty"</code></li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">操作步骤</h2>
  <ol style="margin:0 0 1rem;padding-left:1.2rem">
    <li>粘贴真实接口响应或点示例填充。</li>
    <li>填写根结构体名（需是合法 Go 标识意图，工具会尽量清洗）。</li>
    <li>按需要勾选 omitempty / 内联类型，点「转换」。</li>
    <li>检查字段类型与嵌套结构，点「复制代码」放入项目。</li>
    <li>用 <code>encoding/json</code> 反序列化实测，必要时手动改 <code>json.Number</code> 或指针字段。</li>
  </ol>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">适用场景</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>快速给第三方 API 建模</li>
    <li>把抓包 JSON 落成后端 DTO</li>
    <li>教学演示 JSON 与 Go 类型映射</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">边界与限制</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>数组元素类型以第一个元素为准，异构数组可能不准。</li>
    <li>大整数超过 JS 安全整数时可能失真（浏览器 JSON.parse 限制）。</li>
    <li>不会生成校验标签、不会写构造函数或示例 main。</li>
    <li>同名结构体会尽量去重，复杂图结构仍需人工整理。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">常见问题</h2>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">转换失败提示 JSON 错误？</h3>
  <p style="margin:0 0 .8rem">先保证输入是合法 JSON：键必须双引号、不能有注释与尾逗号。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">为什么数字变成 float64？</h3>
  <p style="margin:0 0 .8rem">当 <code>Number.isInteger</code> 为 false 时用 float64。若业务需要定点小数，请手改类型。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">内联类型适合什么时候开？</h3>
  <p style="margin:0 0 .8rem">结构浅、只想要一个文件内匿名结构时开启；深嵌套通常关闭以生成多个命名 struct。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">会上传我的 JSON 吗？</h3>
  <p style="margin:0 0 .8rem">转换在浏览器本地执行，适合处理未公开接口样例。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">字段名里的下划线怎么处理？</h3>
  <p style="margin:0 0 .8rem"><code>user_id</code> 会变成导出字段 <code>UserId</code> 一类 PascalCase，json tag 仍保留原键名。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">根是数组怎么办？</h3>
  <p style="margin:0">若数组元素是对象，会按首元素对象生成结构，并得到切片类型用法；空数组则类型信息有限。</p>
`),
  },

  'tools/life/decision-maker.html': {
    anchor: null,
    beforeFooter: true,
    guide: section(`
  <h2 style="font-size:1.25rem;margin:0 0 .85rem">决策助手完整使用指南</h2>
  <p style="margin:0 0 1rem">决策助手提供四种随机决策玩法：转盘、抛硬币、掷骰子、随机选择。转盘默认三项「选项1/2/3」，可继续添加并在 canvas 上绘制扇区后旋转；硬币给出正面/反面动画；骰子可选 1–3 个，用 Unicode 骰子字符显示；随机模式从你自定义列表里抽取。历史记录区会追加每次结果，便于回看。主题按钮可切换浅色/深色。它适合打破“小选择拖延”，不适合重大法律/医疗/财务决策。</p>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">四个模式分别做什么</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li><strong>转盘</strong>：输入选项点 ➕ 或 Enter 添加；点「开始转动」旋转 canvas 转盘并展示结果。</li>
    <li><strong>抛硬币</strong>：点「抛硬币」，硬币翻转动画后给出正/反结果。</li>
    <li><strong>掷骰子</strong>：先选 1/2/3 个骰子，再掷出；结果用 ⚀–⚅ 显示。</li>
    <li><strong>随机</strong>：维护选项列表后点「随机选择」抽取一项。</li>
    <li>底部历史：记录模式与结果文本，方便对比多次尝试。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">推荐用法</h2>
  <ol style="margin:0 0 1rem;padding-left:1.2rem">
    <li>先把“真正可接受”的选项写全，再开始随机，避免抽到不可执行项。</li>
    <li>二选一用硬币；多选项用转盘或随机列表。</li>
    <li>需要一点仪式感时用转盘动画，需要数字结果时用骰子。</li>
    <li>重大决策请先做利弊表，把本工具仅作破冰，不作为唯一依据。</li>
  </ol>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">适用场景</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>午饭吃什么、周末去哪、看哪部影片</li>
    <li>团队破冰小游戏、抽签决定发言顺序</li>
    <li>在多个都可行的方案里快速推进</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">边界</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>结果由前端伪随机产生，不具备密码学随机强度。</li>
    <li>不是加权决策：默认各选项等权（除非页面另有权重实现）。</li>
    <li>历史记录在页面会话中展示，刷新后通常不作为长期档案。</li>
    <li>不能替代正式投票系统与可审计抽签流程。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">常见问题</h2>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">转盘没选项能转吗？</h3>
  <p style="margin:0 0 .8rem">默认有三个示例选项；若删到为空，请先添加至少一项再转。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">骰子最多几个？</h3>
  <p style="margin:0 0 .8rem">界面提供 1/2/3 个切换，不是任意数量。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">能给选项设置不同概率吗？</h3>
  <p style="margin:0 0 .8rem">当前是娱乐向等权随机，不做复杂权重配置面板。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">结果可复现吗？</h3>
  <p style="margin:0 0 .8rem">一般不可复现。若要可审计抽签，请使用带种子与公示规则的专业流程。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">适合做公司制度决策吗？</h3>
  <p style="margin:0">不适合。请用于低风险个人/轻松场景，重要事项走会议与数据评估。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">主题切换影响结果吗？</h3>
  <p style="margin:0">不影响随机逻辑，只改变界面明暗样式。</p>
`),
  },

  'tools/office/signature-pad.html': {
    anchor: null,
    beforeFooter: true,
    guide: section(`
  <h2 style="font-size:1.25rem;margin:0 0 .85rem">电子签名板完整使用指南</h2>
  <p style="margin:0 0 1rem">本页提供浏览器手写签名画布：可调笔颜色、笔粗细，支持透明背景导出，操作包含「下载签名 (PNG)」「撤销一笔」「清空画布」。笔画保存在内存数组 <code>paths</code> 中，撤销会移除最后一笔并重绘。画布按容器宽度与 <code>devicePixelRatio</code> 缩放以保证清晰度，支持鼠标与触摸。签名在本地 canvas 完成，适合快速生成签名图片，不构成法律意义上的数字证书签名。</p>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">控件说明</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li><code>#penColor</code>：笔迹颜色。</li>
    <li><code>#penWidth</code>：线宽滑杆，旁侧 <code>#widthVal</code> 显示数值。</li>
    <li><code>#checkTransparent</code>：导出是否透明背景。</li>
    <li><code>#btnDownload</code>：导出 PNG。</li>
    <li><code>#btnUndo</code>：撤销最近一笔路径。</li>
    <li><code>#btnClear</code>：清空全部路径。</li>
    <li>绘制：按下开始记录点列，移动描边，抬起结束一笔并压入 <code>paths</code>。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">推荐步骤</h2>
  <ol style="margin:0 0 1rem;padding-left:1.2rem">
    <li>先选颜色与线宽，在画布上签写姓名或花签。</li>
    <li>写错用「撤销一笔」，不要的整板内容用「清空」。</li>
    <li>若要贴到深色/复杂背景文档，勾选透明背景再下载。</li>
    <li>下载 PNG 后插入 Word/PDF/表单；需要矢量签名请另用专业工具。</li>
  </ol>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">适用场景</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>内部流程单据快速生成签名图</li>
    <li>演示、教学材料中的手写署名</li>
    <li>触控板/手机上手写后导出</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">法律与安全边界</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>这是图像签名，不是 PKI/证书数字签名，不提供不可抵赖保证。</li>
    <li>重要合同请遵循当地电子签名法规与平台要求。</li>
    <li>签名图片可能被复制粘贴，注意使用场景与水印策略。</li>
    <li>笔画仅在当前页面内存；刷新会丢失未下载内容。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">常见问题</h2>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">手机上能签吗？</h3>
  <p style="margin:0 0 .8rem">支持触摸事件坐标；建议横屏并提高线宽以便书写。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">为什么导出发糊？</h3>
  <p style="margin:0 0 .8rem">页面已按 devicePixelRatio 缩放；若仍糊，可能是插入文档时被二次拉伸，请按原始像素放置。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">撤销能多步吗？</h3>
  <p style="margin:0 0 .8rem">可以连续点撤销，每次去掉最后一笔；没有“反撤销/重做”栈。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">透明背景不生效？</h3>
  <p style="margin:0 0 .8rem">确认已勾选透明选项后再下载；部分查看器会把透明显示成黑/白底，请用支持 alpha 的软件打开。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">签名会上传吗？</h3>
  <p style="margin:0">绘制与导出在本地 canvas 完成，下载到你的设备；请自行保管生成的 PNG。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">能导出 SVG 吗？</h3>
  <p style="margin:0">当前主流程是 PNG 下载，不是 SVG 路径导出。</p>
`),
  },

  'tools/life/world-clock.html': {
    anchor: null,
    beforeFooter: true,
    guide: section(`
  <h2 style="font-size:1.25rem;margin:0 0 .85rem">世界时钟完整使用指南</h2>
  <p style="margin:0 0 1rem">世界时钟用 IANA 时区名（如 <code>Asia/Shanghai</code>、<code>America/New_York</code>）显示多城市当前时间与日期。城市列表保存在 <code>localStorage.world_clock_cities</code>；默认含纽约、伦敦、东京。你可从下拉添加城市，删除不需要的卡片。时间通过 <code>toLocaleTimeString/DateString</code> 的 <code>timeZone</code> 选项计算，并可用小时判断日夜样式。页面每秒刷新，适合对照协作方当地时间，不负责会议重叠算法。</p>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">功能对照</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>城市字典 <code>cityNames</code>：预置纽约/洛杉矶/伦敦/巴黎/上海/香港/新加坡/东京/悉尼等。</li>
    <li>添加：选择时区后加入 <code>cities</code> 数组并保存。</li>
    <li>删除：从数组移除并写回 localStorage。</li>
    <li>显示：本地化日期 + 24 小时制时分秒。</li>
    <li>日夜：根据该时区当前小时切换卡片视觉（白天/夜晚）。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">推荐步骤</h2>
  <ol style="margin:0 0 1rem;padding-left:1.2rem">
    <li>保留与你协作相关的 3–6 个城市，删掉无关项减少干扰。</li>
    <li>约会议前同时看对方是否处于深夜/清晨。</li>
    <li>跨日场景同时看日期，不只看钟点。</li>
    <li>需要“共同工作小时”计算时，改用会议时间协调器。</li>
  </ol>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">适用场景</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>远程团队日常对时</li>
    <li>客服按客户当地时间回复</li>
    <li>出差跨日航班前后的日期确认</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">边界</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>依赖浏览器时区数据库与系统时间准确性。</li>
    <li>预置列表有限，不包含全世界全部城市。</li>
    <li>不做夏令时规则讲解，只展示当前换算结果。</li>
    <li>不是闹钟/提醒工具，刷新后台标签页可能影响你的查看节奏。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">常见问题</h2>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">为什么和手机差一分钟？</h3>
  <p style="margin:0 0 .8rem">以本机系统时钟为准。请先校准设备时间，再比较。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">夏令时会自动变吗？</h3>
  <p style="margin:0 0 .8rem">使用 IANA 时区名时，浏览器会按规则换算；极端历史规则以运行环境为准。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">城市列表会同步到其他设备吗？</h3>
  <p style="margin:0 0 .8rem">不会。只存在当前浏览器的 <code>world_clock_cities</code>。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">能显示秒吗？</h3>
  <p style="margin:0 0 .8rem">时间格式包含时分秒，并定时刷新。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">和会议协调器如何分工？</h3>
  <p style="margin:0">世界时钟看“现在几点”；会议协调器算“哪几个 UTC 小时大家同时上班”。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">没有我的城市怎么办？</h3>
  <p style="margin:0">选择同一时区的代表城市，或使用系统时区名相近项；页面预置列表以外的自定义输入取决于是否提供自由输入控件。</p>
`),
  },

  'tools/media/image-compare.html': {
    anchor: '<!-- SEO 内容 -->',
    guide: section(`
  <h2 style="font-size:1.25rem;margin:0 0 .85rem">图片对比滑块完整使用指南</h2>
  <p style="margin:0 0 1rem">专业图片对比工具用于观察处理前后差异。上传图片 A（Before）与图片 B（After）后，可在滑块对比、并排显示等模式间查看。滑块模式通过拖拽分割线控制 Before 覆盖宽度；并排模式左右同时展示。未上传齐两张图时显示空状态提示。图片在浏览器本地预览，适合修图前后、压缩前后、设计改版对照，不自动计算像素级差异报告。</p>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">界面结构</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>上传卡 A/B：点击触发隐藏 file input，选择后显示预览缩略图。</li>
    <li>滑块视图：底层 After、上层 Before 裁切，中线手柄可拖。</li>
    <li>并排视图：左右两栏分别显示 A/B。</li>
    <li>空状态：提示“请上传两张图片以开启对比”。</li>
    <li>模式切换：页面提供滑块/并排（及可能的透明混合）控件，以界面按钮为准。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">推荐步骤</h2>
  <ol style="margin:0 0 1rem;padding-left:1.2rem">
    <li>先上传原图到 A，再上传处理后图到 B，保持构图尽量一致。</li>
    <li>在滑块模式左右拖动手柄，观察边缘、噪点、色彩变化。</li>
    <li>需要同时看全图时切并排模式。</li>
    <li>对比结束可保留截图用于评审，本页不强制保存历史。</li>
  </ol>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">适用场景</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>磨皮/滤镜/超分前后效果验收</li>
    <li>压缩画质对比（体积 vs 清晰度）</li>
    <li>UI 改版、海报迭代的视觉 diff</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">边界</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>不输出自动“差异热力图”，需人眼观察。</li>
    <li>两图分辨率不同时，以容器 object-fit 展示，可能出现裁切或留白。</li>
    <li>大图预览受内存限制，可能较慢。</li>
    <li>本地预览，不替代版本管理系统。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">常见问题</h2>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">只上传一张为什么不能对比？</h3>
  <p style="margin:0 0 .8rem">需要 A、B 都就绪才进入有效对比；否则显示空状态。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">滑块拖不动？</h3>
  <p style="margin:0 0 .8rem">确认两图已加载完成，并在对比容器内拖拽手柄区域。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">图片会上传服务器吗？</h3>
  <p style="margin:0 0 .8rem">预览基于本地文件读取，用于前端展示对比。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">能对比 PDF 吗？</h3>
  <p style="margin:0 0 .8rem">请先导出为图片。输入 accept 为 image/*。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">和文本 diff 有何不同？</h3>
  <p style="margin:0">文本 diff 比字符串行；本页比像素画面观感，不做文本算法。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">如何得到更公平的画质对比？</h3>
  <p style="margin:0">尽量使用同构图、同裁切、同显示尺寸，只改变处理参数。</p>
`),
  },

  'tools/media/image-format-converter.html': {
    anchor: '<!-- SEO 内容 -->',
    guide: section(`
  <h2 style="font-size:1.25rem;margin:0 0 .85rem">图片格式批量转换完整指南</h2>
  <p style="margin:0 0 1rem">图片格式批量转换器支持在浏览器本地将多张图片转换为常见目标格式（页面定位包含 WebP、AVIF、PNG、JPEG 等，以界面可选格式为准）。拖拽或点击上传多文件后，在结果区查看每张转换状态，并可「下载全部 (ZIP)」。处理强调本地完成以保护隐私，适合把相册/素材统一成网页友好格式，而不是专业色彩管理印刷流程。</p>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">界面模块</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>拖拽区 <code>#dropZone</code> + 隐藏多选 <code>#fileInput</code>。</li>
    <li>状态文案 <code>#statusMsg</code> 提示当前进度/结果。</li>
    <li>结果区 <code>#resultSection</code>：显示文件数 <code>#fileCount</code> 与网格 <code>#resultGrid</code>。</li>
    <li>「下载全部 (ZIP)」调用 <code>downloadZip()</code> 打包导出。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">推荐步骤</h2>
  <ol style="margin:0 0 1rem;padding-left:1.2rem">
    <li>选择目标格式与质量参数（若页面提供质量滑杆/选项）。</li>
    <li>一次拖入多张图片，等待全部转换完成。</li>
    <li>在结果网格预览/单张下载，或打包 ZIP 一次带走。</li>
    <li>上线前抽查透明通道、色彩与体积是否符合预期。</li>
  </ol>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">适用场景</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>网站静态资源统一为 WebP/AVIF 以减小体积</li>
    <li>PNG 图标与 JPEG 照片批量互转</li>
    <li>交付前把混合格式素材整理成同一格式</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">格式与兼容边界</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>AVIF/WebP 编码能力取决于浏览器实现，老旧浏览器可能失败。</li>
    <li>JPEG 不支持透明；透明图转 JPEG 会丢 alpha。</li>
    <li>多次有损互转会累积画质损失，尽量基于原图一次转出。</li>
    <li>超大图/超多文件受内存限制，可能需要分批。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">常见问题</h2>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">为什么 ZIP 按钮点了没反应？</h3>
  <p style="margin:0 0 .8rem">确认至少有一张转换成功；若仍在转码，等状态完成后再打包。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">图片会上传吗？</h3>
  <p style="margin:0 0 .8rem">页面定位为本地处理；文件在浏览器中解码再导出。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">透明背景变黑/白？</h3>
  <p style="margin:0 0 .8rem">目标格式若不支持透明（如 JPEG），透明区域会被填充。需要透明请用 PNG/WebP（视实现）。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">能保留 EXIF 吗？</h3>
  <p style="margin:0 0 .8rem">canvas 重编码通常会丢失大部分 EXIF；若需保留元数据请用专门工具。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">和“图片尺寸修改”如何配合？</h3>
  <p style="margin:0">先缩放再转格式，或先转格式再缩放；避免无意义的多次有损链。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">单次最多多少张？</h3>
  <p style="margin:0">无严格写死上限时以设备内存为准。卡顿就减少批量，分多次转换。</p>
`),
  },

  'tools/media/color-picker.html': {
    anchor: '<!-- SEO 内容 -->',
    guide: section(`
  <h2 style="font-size:1.25rem;margin:0 0 .85rem">在线取色器完整使用指南</h2>
  <p style="margin:0 0 1rem">颜色选择器用于获取与转换颜色值。页面提供大号预览块、HEX 输入/显示，以及 RGB、HSL 只读或可复制字段；常见交互包括点击复制 HEX、RGB、HSL。部分实现还支持系统 <code>EyeDropper</code> API 从屏幕取色（浏览器支持时）。适合设计标注、CSS 变量整理与快速配色，不替代完整色彩管理体系（ICC/印刷专色）。</p>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">你能在页面上做什么</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>在色板/取色控件中选择颜色，预览实时更新。</li>
    <li>查看 HEX（如 <code>#3B82F6</code>）并一键复制。</li>
    <li>查看 RGB 分量（0–255）与 HSL 表示，便于写 CSS。</li>
    <li>若有“屏幕取色/吸管”按钮且浏览器支持，可吸取桌面任意像素颜色。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">推荐步骤</h2>
  <ol style="margin:0 0 1rem;padding-left:1.2rem">
    <li>从设计稿或灵感图确定目标色，用选择器或吸管获取。</li>
    <li>复制 HEX 给 CSS/设计工具；需要半透明时再手动加 alpha（若页面未提供 alpha）。</li>
    <li>把主色、悬停色、背景色成组记录，避免同一产品里颜色漂移。</li>
    <li>无障碍场景请再检查对比度（本页若不提供对比度计算，请另用检测工具）。</li>
  </ol>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">适用场景</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>写 CSS 时快速拿到 HEX/RGB</li>
    <li>从截图/网页吸取品牌色</li>
    <li>整理设计 token 前的颜色采集</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">边界</h2>
  <ul style="margin:0 0 1rem;padding-left:1.2rem">
    <li>屏幕取色依赖浏览器权限与 API 支持，不支持时按钮可能不可用。</li>
    <li>不同显示器校色会导致“肉眼不一致”，以数值为准。</li>
    <li>不一定包含 CMYK/专色；印刷请用专业流程。</li>
    <li>复制的是颜色字符串，不会自动写入你的代码仓库。</li>
  </ul>

  <h2 style="font-size:1.12rem;margin:1.35rem 0 .55rem">常见问题</h2>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">HEX 可以带 # 吗？</h3>
  <p style="margin:0 0 .8rem">页面展示通常含 <code>#</code>；粘贴到某些工具时按对方要求保留或去掉。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">为什么吸管不能用？</h3>
  <p style="margin:0 0 .8rem">需要 Chromium 系等支持 EyeDropper 的环境，且用户手势触发；HTTP/权限策略也可能影响。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">RGB 和 HSL 哪个写 CSS 更好？</h3>
  <p style="margin:0 0 .8rem">看团队规范。调亮度时 HSL 更直观；与设计稿对色常用 HEX/RGB。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">能保存色板吗？</h3>
  <p style="margin:0 0 .8rem">以页面是否提供历史/收藏为准；若无持久化，请自行复制到文档。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">取值会上传吗？</h3>
  <p style="margin:0">颜色计算在本地完成，复制动作也在浏览器内。</p>
  <h3 style="font-size:1.02rem;margin:1rem 0 .35rem">和图片取色（从上传图取色）有何不同？</h3>
  <p style="margin:0">若本页主打通用选择器/屏幕取色，则不依赖上传图片；从图片像素取样请用带图片画布的取色工具页。</p>
`),
  },
};

function inject(html, page) {
  html = html.replace(/<section class="tool-guide"[\s\S]*?<\/section>\s*/i, '');
  const block = '\n' + page.guide.trim() + '\n';
  if (page.anchor && html.includes(page.anchor)) {
    return html.replace(page.anchor, block + page.anchor);
  }
  if (html.includes('</main>')) {
    return html.replace('</main>', block + '</main>');
  }
  if (html.includes('<footer')) {
    return html.replace('<footer', block + '<footer');
  }
  throw new Error('no injection point');
}

let ok = 0;
const report = [];
for (const [rel, page] of Object.entries(PAGES)) {
  const file = path.join(root, rel);
  const original = fs.readFileSync(file, 'utf8');
  const guideCn = cn(page.guide);
  const next = inject(original, page);
  fs.writeFileSync(file, next, 'utf8');
  const body = (next.match(/<body[\s\S]*<\/body>/i) || [next])[0];
  const pageCn = cn(body);
  const h1 = (next.match(/<h1\b/gi) || []).length;
  report.push({ rel, guideCn, pageCn, h1 });
  console.log(`${rel}: guideCn=${guideCn} pageCn=${pageCn} h1=${h1}`);
  if (guideCn < 800 || guideCn > 1300) console.warn('OUT_OF_RANGE', guideCn, rel);
  if (h1 !== 1) console.warn('H1_COUNT', h1, rel);
  ok += 1;
}
console.log('done', ok);
const short = report.filter((r) => r.guideCn < 800);
if (short.length) {
  console.log('SHORT', short);
  process.exitCode = 2;
}
